spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:safefam;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop

  flyway:
    enabled: false

kakao:
  client-id: test-client-id
  user-info-url: https://kapi.kakao.com/v2/user/me

solapi:
  api-key: test
  api-secret: test
  sender: "01000000000"

firebase:
  service-account-path: test